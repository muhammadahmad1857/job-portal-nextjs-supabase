import React from "react";
import ProtectedRoute from "@/components/protected-route"
const Job = () => {
  return (
    <ProtectedRoute>

  <div>Slug Page job</div>
  </ProtectedRoute>
  );
};

export default Job;
